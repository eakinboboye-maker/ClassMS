# JupyterLite Deployment Notes

Add the JupyterLite site origin to backend `CORS_ORIGINS`.

Example:

```env
CORS_ORIGINS=https://teacher.example.com,https://exam.example.com,https://learn.example.com
```
