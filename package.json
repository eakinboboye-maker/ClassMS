{
  "name": "classlite-jupyterlite-portal-upgrade",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "python -m jupyter lite build --config jupyter_lite_config.json --output-dir dist && python scripts/copy_portal.py && ls dist && ls dist/assets",
    "clean": "rm -rf dist .jupyterlite.doit.db",
    "rebuild": "npm run clean && npm run build",
    "serve": "python -m http.server 9000 -d dist",
    "serve_headers": "npx static-handler --cors --coop --coep --corp ./dist"
  }
}